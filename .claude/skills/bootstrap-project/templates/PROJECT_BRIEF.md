# Project Brief — <PROJECT_NAME>

<!--
Emitted by bootstrap-project (Step 0). This is a COMPRESSED Phase −1 brief: just
enough to know what's being built before the stack is wired. The short bootstrap
survey pre-fills what you answered; everything left as a placeholder is for you to
fill THOUGHTFULLY in the editor (not the terminal) — or to ask the operator.

Skipped a question at bootstrap? Its field reads "(not specified)" — that's fine,
fill it later. Told bootstrap "the client knows that"? The question was reformulated
for the owner and collected in `CLIENT_QUESTIONS.md` (repo root, in the client's
language) — send that file to them. Nothing here blocks building.

Full discovery method (questionnaire + outside-in / inside-out playbooks) lives in
`docs/discovery/`. This brief is the on-ramp to it, not a replacement.
-->

## What we're building (one sentence)

<one phrase — what this is>

## Who it's for / what pain it solves

- **Audience:** <who uses this>
- **Pain / job-to-be-done:** <the concrete problem it removes>

## Positioning (one-liner)

<the "for <whom>, this is <what>, unlike <alternative>" line — fill thoughtfully>

## Discovery mode

<one of: inside-out (own brand, you know the pain) / outside-in (someone else's
business, researched cold) / skipped>

> Method & templates: `docs/discovery/` — CLIENT_INTAKE_QUESTIONNAIRE (modes A/B),
> INTAKE_PLAYBOOKS, positioning/tone templates. This brief is the compressed entry
> point; the full Phase −1 lives there.

## Notes / open questions

<anything else that shapes what gets built — constraints, deadlines, non-goals>
