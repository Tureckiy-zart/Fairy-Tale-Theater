# SEO_SCAFFOLD — <PROJECT>

> Phase −1 deliverable. A discovery/keyword layer tuned to how **this** buyer
> searches. Keep discovery tags decoupled from the brand's visual language.

## Keyword sets

**Primary (intent):**
- `<term>`, `<term>`, `<term>`

**Secondary (reach / discovery tags):**
- `<term>`, `<term>` — *discovery only, not the visual/brand language.*

## Mode shape

- **Inside-out → category/intent keywords** (what the buyer types when they want
  this category).
- **Outside-in → local / map-pack SEO** (GBP completeness, NAP consistency, review
  recency, "near me" + local terms like `<town/postcode/landmark>`).

## Connector targets (not wired in discovery)

A single SEO connector (**Semrush OR Ahrefs**, operator's choice) turns this from a
checklist into ranked targets: keyword volume/difficulty, competitor keyword gap,
backlink gap, rank tracking. Note here what it should pull: `<...>`.

## Decoupling rule

Keep reach-tags (`<e.g. a trend label>`) **only** as discovery tags for reach —
**not** the brand's visual or copy language.

## Confidence

- `<HIGH/MED/LOW>`.
