# TONE_VOICE — <PROJECT>

> Phase −1 deliverable. The voice is the language the subject already uses.
> Reverse-engineer it from real words; don't invent a "brand voice."

## Voice formula

- Lead with: `<the calm/careful/unhurried/… quality the language reveals>`.
- Sounds like: `<3–5 adjectives drawn from the real phrases>`.
- Never sounds like: `<corporate / hypey / generic marketing>`.
- Translation principle: **translate the state, not the words** — keep the feeling,
  not the literal phrasing.

## Source phrases → tone

> "<real phrase>"  → conveys `<the state to preserve in all copy>`.

## Copy test (à la a real own-brand launch)

A draft passes only if:

1. Would the voice-source recognise themselves in this text?
2. Is there a single invented fact? (must be **no**)
3. Does it lead with the positioning, not a feature list?
4. Any hype/guarantee not backed by evidence? (must be **no**)
5. Could a competitor paste their name on it? (must be **no** — it must be specific)
6. `<project-specific check>`

## Mode note

- Inside-out yields a **rich** voice (a living person). Outside-in is **leaner**
  (review vocabulary) — do not claim outside-in produces own-brand-depth tone.

## Confidence

- `<HIGH/MED/LOW>`.
