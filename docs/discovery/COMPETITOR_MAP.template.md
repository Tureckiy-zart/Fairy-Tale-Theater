# COMPETITOR_MAP — <PROJECT>

> Phase −1 deliverable. Map the field, locate the exploitable gap.

## Rivals

| Rival | Rating · reviews / category · price | Where they're strong | Where they're weak |
|---|---|---|---|
| `<name>` | `<4.6 · 312 / mid-band>` | `<...>` | `<rushed, closed Sundays, diluted>` |
| `<name>` | `<...>` | `<...>` | `<...>` |

> Inside-out: comparable **brands** (category, price band, proven mechanics).
> Outside-in: local rivals via `places_search` (rating / hours / review counts).

## Where we win / where we're exposed

- **Win:** `<the specific thing only we do>`.
- **Exposed:** `<where rivals are stronger>`.

## The gap to own

`<one specific, defensible gap — e.g. "the unhurried option in a market of
conveyor-belt cuts" / "nettle+hemp, not only linen, one maker, a real place">`.

## Data layer used

- Web/`places_search`: `<what it covered>`.
- SEO connector (Semrush/Ahrefs) needed for: keyword/backlink/SERP depth,
  share-of-voice — `<run? not wired?>`.

## Confidence & gaps

- Confidence: `<HIGH/MED/LOW>`. Sample size / unverified: `<...>`.
