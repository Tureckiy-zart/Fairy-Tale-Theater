# PROJECT PROGRESS — Miss Lana's Fairy-Tale Theatre

> **Назначение.** Append-only **полный лог всех содержательных изменений** проекта.
> **Правило (`CLAUDE.md` §7):** в конце КАЖДОЙ задачи добавляется краткая запись — дата + 1–3 строки «что сделано» (затронутые файлы/артефакты + суть).
> **НЕ логируется:** прогоны CI/тестов, отладка/работа с ошибками.
> **Роли:** `STATUS.md` — текущее состояние (snapshot); **этот файл — хронология** (только дополнять, не переписывать задним числом).
> **Формат:** `## YYYY-MM-DD` → буллеты. Новые записи добавляются ВНИЗ соответствующей даты.

---

## Baseline (по артефактам репозитория и git-истории — до введения этого лога)

- **Лончеры и README.** Добавлены Windows/Linux-лончеры; README переписан под Variant A; `launch.bat` отклоняет WSL/System32-bash в PATH-fallback. *(git: PR #1/#2, commits `6b2df90`, `e2e6b36`, `5a100e4`, `4fd3bc4`.)*
- **Intake.** Двухуровневый discovery-опрос, клиентский язык, site-vision, деградация при отсутствии онлайн-следа. *(git: `0600fbe`.)*
- **Ребрендинг-рамка зафиксирована (2026-06-22, core-доки).** `docs/core/BRAND.md` (LOCK): бренд **Miss Lana's Fairy-Tale Theater**, зонтик **Miss Lana**, домен `misslanatheater.com`, 4 линии-секции. Переписаны под актуальную рамку: `PROJECT_BRIEF.md`, `00_PROJECT_INSTRUCTIONS.md`, `01_CONTENT_INVENTORY.md`, `02_POSITIONING_AND_TONE.md` (v5), `03_SITEMAP_AND_SCOPE.md`, `04_SEO.md` — англо-первичный рынок, живой костюмированный театр (не «puppet»), украинская труппа как тихий бэкстори, без славянского кодирования.
- **Research-артефакты (Hermes, 2026-06-21).** `HermesResearch/reports/2026-06-21-la-kids-puppet-theater-competitor-research.md` (карта конкурентов и нормы цен) · `2026-06-21-google-maps-platform-scoping.md` (Трек B — Google Maps Platform).
- **Trademark pre-screen.** `docs/2026-06-22-trademark-pre-screen-miss-lanas-fairy-tale-theater.md`.

---

## 2026-06-22

- **Дизайн-система установлена.** `docs/core/DESIGN_SYSTEM.md` (DS v1.0, идея «Lantern Light»: лесные зелёные + золотисто-янтарное свечение на кремовом) — источник истины по визуалу; обоснование/ресёрч — `docs/core/2026-06-22-design-system-research-v2.md`.
- **Design System Research (TUNG `HERMES_DESIGN_SYSTEM_RESEARCH_001`).** Создан decision-ready отчёт `HermesResearch/reports/2026-06-22-design-system-research.md`: рекомендованное направление «Lantern Light» (в отчёте — рабочее имя) + 2 альтернативы; токены color/type/spacing/radius/elevation/motion с детерминированно проверенным WCAG AA контрастом; направление иллюстрации/иконок (Phosphor) + персонажа Miss Lana; шрифты Fraunces + Nunito (web-verified). Спека, не реализация; лого — только направление (TM pending).
- **Переименование.** `docs/core/design-system-research.md` → `docs/core/2026-06-22-design-system-research-v2.md` (каноническое date-prefixed имя; починены ссылки из `DESIGN_SYSTEM.md`).
- **Интеграция дизайн-системы.** `docs/core/DESIGN_SYSTEM.md` прописан как always-on источник истины по визуалу в `CLAUDE.md` (§3 указатель + новая §8) и `docs/core/00_PROJECT_INSTRUCTIONS.md` (список «Источники истины» + директива «Как работаешь»).
- **Правило лога прогресса.** В `CLAUDE.md` §7 добавлено правило вести `docs/PROJECT_PROGRESS.md`; создан этот файл и заполнен текущим состоянием.
- **Дизайн-превью построено (TUNG `BUILD_MISS_LANA_DESIGN_PREVIEW_001`).** Первый срез сборки в `baselines/js-next/` (единственное Next.js-приложение репо — подтверждено владельцем как app сайта). Внутренний noindex-маршрут `/design` рендерит 9 секций дизайн-системы (color/type/spacing/icons/motif/motion/components/accents/a11y) строго по `docs/core/DESIGN_SYSTEM.md`. Заведена токен-основа: `app/globals.css` (Tailwind v4 `@theme static` — все токены §3/§4/§5/§10/§14 + base-layer + motion-keyframes + reduced-motion бэкстоп), `app/layout.tsx` (`next/font`: Fraunces+Nunito), `phosphor-react` в `package.json`. Добавлен e2e-smoke `tests/e2e/design.spec.ts`. Гейт `pnpm run ci:exact` + `pnpm test:e2e` — зелёные. How-to-view — в `baselines/js-next/README.md`; ревью/находки — `docs/2026-06-22-design-preview-build-findings.md`. Перед продом маршрут `/design` env-guard'ить/удалить.
- **Продакшн-примитивы реализованы (TUNG `IMPLEMENT_MISS_LANA_DESIGN_TOKENS_001`).** Добавлена переиспользуемая, типизированная, доступная UI-библиотека `baselines/js-next/components/ui/`: `Button` (primary/secondary/tertiary, sm/md/lg, leading/trailing icon, polymorphic button/`<a>`), `Field` (label+helper/error/success, `(required)`, textarea — presentational), `Card` (show-card 3:2 + meta + CTA), `Nav` (sticky, scroll-state via IO-sentinel, mobile drawer с focus-trap+ESC+scroll-lock, skip-link); + `cx` helper, `index` barrel, `README.md` (usage/props). Все стили — из единой токен-основы (§14), без дублирования/ad-hoc значений; иконки только Phosphor (Regular UI + Duotone-brand), ≤2 начертания; motion-safe-first. **Repoint `/design`:** `components-section.tsx` теперь рендерит реальные примитивы (живая галерея), `page.tsx` использует реальный `Nav` как шапку (удалён hand-rolled header), маршрут остаётся внутренним noindex. **Hardening основы:** убран `border-radius:2px` из `:focus-visible` в `globals.css` (пилюли/карточки сохраняют форму при фокусе). e2e `design.spec.ts` расширен (рендер примитивов + клавиатурный фокус/skip-link + drawer open/close/ESC/focus-restore). Гейт `pnpm run ci:exact` + `pnpm test:e2e` — зелёные (6/6). Остаётся за trademark-гейтом: финальные лого/вордмарк/персонаж/иллюстрации (примитивы работают на плейсхолдерах).
- **Развёртывание в формальный проект (2026-06-22).** Проект перенесён из рабочего клона кита (`Magic-castle`) в отдельную папку `Fairy-Tale-Theater`, развёрнутую из `factory-starter-kit` (JS baseline): код примитивов и `/design` подняты из `baselines/js-next/*` в корень (`app/`, `components/`, `tests/`), `phosphor-react` + lock синхронизированы; перенесён канон (`docs/core/`), ресёрч, governance, security; kit-машинерия (`baselines/`, `skills/`, лончеры) не переносилась. Первый коммит → запушен в `git@github.com:Tureckiy-zart/Fairy-Tale-Theater.git` (ветка `main`, заменил прежний снимок).
- **Гигиена структуры (2026-06-22).** Ресёрч-отчёты `HermesResearch/reports/` → `docs/reports/` (ссылки в `CLAUDE.md`/`STATUS.md`/`docs/core/04_SEO.md` починены); референс-фото `_internal/photo/` → `docs/assets/` (убран дублирующий корневой `_internal/`); добавлен `lib/env.ts` (ленивый типобезопасный доступ к env — единственное место чтения `process.env`, как требует governance); дефолтная Vercel-демка `app/page.tsx` заменена нейтральной WIP-заглушкой, удалены неиспользуемые `public/*.svg`; корневой `README.md` переписан как индекс проекта (был baseline-readme кита).
- **Расширение примитивов (TUNG `EXTEND_MISS_LANA_PRIMITIVES_001`, 2026-06-22).** `components/ui/`: **Card** получил опциональную Button-CTA (`cta`) + service-line `accent` (§12) + `tag` (show-card текст-ссылка сохранена); новые **Breadcrumb** (+ BreadcrumbList JSON-LD), **Tag**, **Accordion** (FAQ, без layout-анимации §10.4), **Container/Section/SectionHeader** (§5.1/§11), **JsonLd** (без `dangerouslySetInnerHTML`), `accent.ts` (§12). **`lib/seo.ts`** — `buildMetadata` + схемы PerformingGroup/LocalBusiness, TheaterEvent, BreadcrumbList, FAQPage. Барелл/README/`/design`-галерея/`design.spec.ts` обновлены. Разрешён конфликт с параллельной Phase-1-сессией: единый источник примитивов сведён к `@/components/ui` (по решению владельца). Гейт `ci:exact` + `pnpm test:e2e` зелёные (e2e 19/19). Коммит — только примитивы; Phase-1 (shell/blocks/Home) фиксирует параллельная сессия.
- **Phase 1: каркас + Home + Booking + Pricing (TUNG `BUILD_MISS_LANA_HOME_AND_SHELL_001`, 2026-06-22).** Глобальный каркас `components/shell/` (`SiteShell`/`SiteHeader`/`SiteFooter`/`BookingCTABand`/`LeadForm` — клиентская валидация + on-screen confirmation, без бэкенда/`StubPage`) + блоки Home `components/blocks/` (Hero=LCP без анимации, TrustStrip, FormatExplainer, ServiceLineCards, FeaturedShows, B2B/B2C-тизеры, PersonaIntro, GalleryTeaser, HowItWorksAreas) + helpers `components/motion/Reveal` (канон `.ll-reveal`/IO), `components/brand/Glyphs` (server-safe мотив света), факты `lib/site.ts`. Маршруты: `app/page.tsx` (полный стек §4.1), `app/booking/` (§4.7), `app/pricing/` (§4.4) + 6 stub'ов (`/shows`,`/school-shows`,`/birthdays`,`/characters`,`/gallery`,`/about`) — nav без 404. Композиция целиком из `@/components/ui` (без форков/дублей) + `lib/seo buildMetadata`; site-wide **noindex** (`app/layout.tsx` + per-page). e2e `tests/e2e/site.spec.ts` (12: страницы 200, nav, валидация+confirmation формы, skip-link/drawer, reduced-motion, noindex). `ci:exact` + `pnpm test:e2e` зелёные (19/19). Всё на плейсхолдерах; placeholder→финал + находки — `docs/reports/2026-06-22-home-and-shell-build-findings.md`.
- **Cleanup долга после Phase 1 (TUNG `CLEANUP_MISS_LANA_POST_PHASE1_001`, 2026-06-22).** Четыре ограниченных пункта, без новых страниц/блоков/редизайна: (1) иконки `phosphor-react@1.4.1` → поддерживаемый `@phosphor-icons/react@2.1.10` (React 19) — мигрированы все 18 импортов (UI/блоки/`/design`), одна библиотека, Duotone-нить (`path[opacity]`→`glow-400`) без правки селектора; (2) `Nav` получил проп `homeHref` (default `/`) — вордмарк ведёт Home (не `href="#"`) + `aria-label`, `SiteHeader` передаёт `homeHref="/"`; (3) `APP_BASE_URL` задокументирован прод-примером в `.env.example` (пламбинг `lib/env`→`lib/seo.absoluteUrl`→canonical/OG/BreadcrumbList уже был, origin не зашит в код); (4) канон: `DESIGN_SYSTEM.md` §6/§14.3 имя пакета + §2 принцип **mobile-first**, `SITE_STRUCTURE_AND_BLOCKS.md` §1 mobile-first, `components/ui/README.md` имя пакета. e2e расширен (вордмарк→Home; Duotone рендерится); `ci:exact` + `pnpm test:e2e` зелёные (21/21). Вне scope (трекнуто): CSP `'unsafe-eval'` dev-warning, консолидация `lib/site`↔`lib/seo`.
- **Preview polish (TUNG `FIX_MISS_LANA_PREVIEW_POLISH_001`, 2026-06-22).** Пять точечных правок, без новых страниц/токенов/редизайна: (1) `<html lang="en-US">` + booking date-поле → маскированное `mm/dd/yyyy` (controlled, без либы) + валидация формата (`LeadForm`); (2) brand-глиф `Lantern` (силуэт фонаря, server-safe SVG) в `Glyphs.tsx` вместо bulb-подобного `Lightbulb` в `BookingCTABand`/`LeadForm` (плейсхолдер; Phosphor остаётся либой иконок); (3) канонические названия featured-шоу в `lib/site.ts` из `01_CONTENT_INVENTORY` (Little Red Riding Hood · The Bunny's Little House · Father Frost · The Gingerbread Man); (4) `«Storybook Grove»` → `«Lantern Light»` в `PROJECT_PROGRESS` + research-отчёте (рабочее имя направления, шедшего как Lantern Light); (5) дружелюбный «Coming soon» в `StubPage` (убран внутренний phase-жаргон + проп `phaseNote`). e2e date-fill → `12/01/2026`; `ci:exact` + `pnpm test:e2e` зелёные (21/21). Параллельно шла Phase-2 сессия (`/shows` стал реальным хабом — мой stub-эдит там перетёрт). Отложено владельцу/Phase-2: price-floor ($300 vs «from $350»), nav «Pricing», длина Home-формы. Находки — `docs/reports/2026-06-22-preview-polish-findings.md`.
- **Phase 2: Shows hub + 8 страниц шоу + лендинги School Shows / Birthdays (TUNG `BUILD_MISS_LANA_SHOWS_AND_LANDINGS_001`, 2026-06-22).** Новый дата-модуль `lib/shows.ts` (8 шоу: slug/title/teaser/synopsis/theme/ages/length/featured — титулы/слаги канон из `01_CONTENT_INVENTORY` + утверждённые переименования: Morozko→`the-winters-gift` (Father Frost), «Well Red Bow wait»→`little-red-riding-hood`; синопсисы — **временный** плейсхолдер-копирайт). Маршруты: `app/shows/page.tsx` (хаб: SectionHeader + `ShowCardGrid`(8) + BookingCTABand), `app/shows/[slug]/page.tsx` (детальный шаблон ×8 через `generateStaticParams`, `dynamicParams=false`: hero+meta+«Book this show» → synopsis/theme → photo/video плейсхолдер → related → areas+«from $350»; **TheaterEvent** + **BreadcrumbList** + уникальная metadata), `app/school-shows/` (B2B-лендинг, sage) и `app/birthdays/` (B2C-лендинг, coral) — оба с FAQ (Accordion + **FAQPage**) + LeadForm + BookingCTABand, заменили stub'ы. Новые блоки `components/blocks/ShowCardGrid.tsx` + `FaqSection.tsx` (композиция из примитивов). Home `FeaturedShows` переведён на `FEATURED_SHOWS` (реальные титулы, ссылки на `/shows/{slug}`); мёртвый `PLACEHOLDER_SHOWS` удалён из `lib/site.ts`. e2e +Phase-2 (200+H1 по 6 маршрутам, TheaterEvent/BreadcrumbList/FAQPage, FAQ-клавиатура, card→detail, unknown-slug 404, noindex). `ci:exact` + `pnpm test:e2e` зелёные (32 passed). **Не строили** (по scope): format-фильтр (owner-gated), суммы доплаты (distance «on request»), backend формы, live-map, финал бренда, реальные фото, индексация (весь сайт **noindex**). Осознанное отступление: фото — не скрейп live-сайта, а помеченный placeholder-трит (нет image-тулинга, CSP `'self'`; fallback по самой задаче); реальные graded-фото — Phase 4 [ASSET]. Находки/🔴 — `docs/reports/2026-06-22-shows-and-landings-build-findings.md`.
- **Phase 3: Services + Characters + Gallery + About — MVP-набор страниц завершён (TUNG `BUILD_MISS_LANA_SHOWCASE_AND_ABOUT_001`, 2026-06-22).** Четыре оставшиеся MVP-страницы на примитивах, mobile-first (заменили stub'ы). `app/services/page.tsx` (зонтик §4.3: hero SectionHeader + 4 service-line `Card` из `SERVICE_LINES` с посекционным акцентом §12 + CTA на свою линию → TrustStrip → BookingCTABand) · `app/characters/page.tsx` («& Friends», **berry** §12: hero placeholder → отстройка от генерик-аниматоров (3 карточки) → how-it-works (3 шага) → BookingCTABand) · `app/gallery/page.tsx` + новый блок `components/blocks/GalleryGrid.tsx` + дата-модуль `lib/gallery.ts` (§4.5: masonry на CSS-columns, 4 канон-категории Shows/Troupe/Children/Backstage, встроенное **muted+captioned видео**; медиа-вайринг как `lib/shows.ts` — `src`/`poster`/`captionsSrc` под Phase 4, до ассетов помеченный «Photo/Video — pending», без скрейпа, reserved aspect = без CLS) · `app/about/page.tsx` (§4.6: миссия → 30+ лет → труппа 4 артиста+роли **канон** из `01_CONTENT` → **тихий flagged бэкстори** (soft default, без славянского кодирования, страна не названа, owner-gated) → `PersonaIntro` (reuse) → ценности → TrustStrip + BookingCTABand). `lib/site.ts`: + `TROUPE` (канон-имена/роли; Svitlana = только Director, владелец **не** утверждается), `VALUES`, footer-ссылка «Services». Каждая страница — один `h1`, уникальная `buildMetadata`, `Breadcrumb` (+ BreadcrumbList JSON-LD). e2e +Phase-3 (200+H1 ×4, noindex ×4, BreadcrumbList ×4, /services→4 линии, gallery scaffold, about troupe/30+, reduced-motion). `pnpm run ci:exact` — зелёный (+4 маршрута). `pnpm test:e2e` — **не выполнить в контейнере** (egress 403, браузер Playwright не скачать; гоняется в CI); замещающая проверка `pnpm dev`+`curl` по 4 маршрутам — зелёная (200/H1/noindex/BreadcrumbList/links/scaffold/troupe). **Не делали** (scope): /blog, реальные ассеты, финал бренд-арта, суммы доплаты, утверждение owner/heritage, индексация. 🔴/находки — `docs/reports/2026-06-22-showcase-and-about-build-findings.md`.
- **Hero-фото (2026-06-23).** В `components/blocks/Hero.tsx` плейсхолдер-панель медиа заменена реальным фото: `next/image` (`fill`, `priority`, `sizes` 50vw/100vw, `object-cover`) в reserved 4:3 боксе (без CLS). Ассет `public/images/hero-girl-curtain.jpg` (1008×756) — кроп 4:3 исходника 1680×756 (девочка в проёме красного занавеса по центру кадра). ⚠️ Источник — фото с сайта конкурента (magic-castle-puppet-theater.com): это **placeholder-трит/direction, не cleared-ассет** (§9/§15, trademark-clearance gate) — под замену реальным фото труппы в Phase 4. Убран неиспользуемый импорт `Tag`. `pnpm typecheck` + `pnpm lint` — зелёные.
- **Портрет Miss Lana в PersonaIntro (2026-06-23).** В `components/blocks/PersonaIntro.tsx` плейсхолдер портрета («Miss Lana — portrait / Character pending», иконка `UserCircle` + `Tag`) заменён реальным фото директора — `next/image` (`fill`, `object-cover object-top`) в существующем квадратном боксе (head-and-shoulders кроп 853×1280, без CLS). Ассет `public/images/lana-01.jpeg` (operator-supplied). Убраны неиспользуемые импорты `UserCircle`/`Tag`. `typecheck` + `lint` зелёные.
- **Hero на высоту экрана (2026-06-23).** В `components/blocks/Hero.tsx` секция на md+ растянута на полный вьюпорт минус шапку (`md:min-h-[calc(100svh-4rem)]`) с вертикальным центрированием (`flex md:items-center`) — первый экран теперь занимает всю высоту. Мобайл (mobile-first) — естественная высота без изменений. `typecheck` + `lint` зелёные.
- **Командное фото труппы на About (2026-06-23).** В `app/about/page.tsx` (секция «Meet the troupe») добавлено реальное групповое фото труппы как презентация команды — `next/image` (`fill`, `object-cover`) в центрированном 4:3-боксе `max-w-3xl` с подписью, над карточками артистов (без CLS). Ассет `public/images/Fairy-Tale Theater.jpg` (operator-supplied, 1280×960). Hero возвращён к `hero-girl-curtain.jpg` (фото труппы решено держать как командный кадр на About, не в hero). Документальный командный кадр — не бренд-стилистика, §8 неприменим (подтверждено владельцем). `typecheck` + `lint` зелёные.
- **Фото в service-line карточки + School Shows hero (2026-06-23).** Card-примитив уже поддерживал `mediaSrc`; в `SERVICE_LINES` (`lib/site.ts`) добавлены опц. поля `media`/`mediaAlt`, прокинуты в `ServiceLineCards` (home) и `app/services/page.tsx`. Заполнены flagship «Fairy-Tale Theater» (фото труппы) и «School Shows» (фото школьного показа). School Shows hero-плейсхолдер («Assembly / classroom photo») заменён реальным фото (`next/image`, 4:3, priority). **Конвертация ассетов:** `Fairy-Tale Theater.jpg` оказался HEIC с расширением .jpg (не рендерился в браузере — отсюда битая картинка) → сконвертирован в `troupe-fairy-tale-theater.jpg` (pillow-heif, 1600×1200); `child-03.jpg` — MPO 5712×4284 5МБ → веб-копия `school-assembly.jpg` (1600×1200). Все ссылки переведены на оптимизированные копии. `typecheck` + `lint` зелёные. Остаются неиспользуемые тяжёлые оригиналы в `public/images/` (HEIC + MPO) — кандидаты на удаление.
- **Фото «Miss Lana & Friends» (2026-06-23).** `IMG_4523.HEIC` (3024×4032, пчёлка-перформер у цветочного занавеса) сконвертирован в JPEG с кропом 3:2 (убрано лишнее небо/провода сверху) → `public/images/miss-lana-friends.jpg` (1600×1067, pillow-heif + crop). Подключён к service-line карточке `friends` (`SERVICE_LINES.media` в `lib/site.ts`) — рендерится на home + /services. HEIC на сайте неприменим (браузеры не рисуют) — отсюда конвертация. Проверено на живом dev (:3000): оптимизатор `200 image/jpeg`. `typecheck` + `lint` зелёные. Остаётся пустой карточка `birthdays`.
- **Birthday-фото + фото на лендинги + ренейминг ассетов (2026-06-23).** (1) `birstday.HEIC` → JPEG-кроп 4:3 (убрано небо/улица) `birthday-party.jpg` (1600×1200), подключён к карточке `birthdays` + hero `/birthdays`. (2) Фото из карточек продублированы в hero соответствующих лендингов: `/characters` ← miss-lana-friends, `/birthdays` ← birthday-party (`/school-shows` уже был). (3) **Переименование ассетов по категориям** (логичные имена вместо стандартных/случайных): `lana-01.jpeg`→`miss-lana-portrait.jpg` (ref в PersonaIntro обновлён), `child-01/02/03`→`audience-children-laughing.jpg`/`show-with-audience.jpg`/`school-show-audience-original.jpg`, `troop-01/02`→`troupe-member-portrait.jpg`/`show-bee-with-child.jpg`, HEIC-оригиналы→`*-original.heic` (`Fairy-Tale Theater.jpg` был HEIC), `tild6566…`→`COMPETITOR-magic-castle-morozko-poster.jpg` (постер конкурента — НЕ для использования). Все ссылки в коде проверены (нет битых). `typecheck`+`lint` зелёные; live :3000 — все фото `200 image/jpeg`, страницы 200. ⚠️ Часть переименованных raw-файлов добавляла параллельная сессия — если она ссылалась на старые имена, поправить.
- **Аудит копирайта сайта (read-only) (2026-06-23).** HERMES_SITE_COPY_AUDIT_001: инвентаризация и проверка ВСЕГО site-facing текста (страницы, shell, блоки, микрокопи, метадата, JSON-LD, копи в `lib/{shows,site,gallery,seo}.ts`) против канона. Отчёт — `docs/reports/2026-06-23-site-copy-audit.md`. Итог: копи в целом on-canon (голос/позиционирование/факты ок, forbidden-grep чист, «The Magic Castle» как тайтл шоу не мисфлагнут). Главное к правке до Phase 4/5: (1) 49 видимых литералов «Placeholder/Temporary copy» в 15 файлах; (2) `PersonaIntro` alt «founder» — owner-gated (Svitlana = только Director); (3) конфликт «from $350» vs строка прайса «$300–350». Owner-needed: апрув 8 синопсисов, прайс-флор, суммы за расстояние, соцссылки. Файлы НЕ менялись (только отчёт). Рекомендация: `BUILD_MISS_LANA_COPY_FIXES_001` (v2-full) для применения копи.

- **Правки копирайта + позиционирование первого экрана (2026-06-23).** Точечные site-facing правки, без новых страниц/редизайна: (1) «Professional troupe of 4» → «Professional troupe» (`TrustStrip`, `lib/site.ts` `FACTS.troupe`) — число артистов не фиксируем. (2) Опыт «30+ лет» переформулирован как опыт театра/руководителя, а не молодых актёров (нестыковка): `school-shows`/`characters` дифференциаторы «Trained actors **with**…» → «**led by** 30+ years of stagecraft», `PersonaIntro` от лица Miss Lana «behind us» → «behind me», `about` meta «troupe with» → «led by». (3) Первый экран hero-лендингов с фото растянут на полный вьюпорт минус шапку (`md:min-h-[calc(100svh-4rem)]`, breadcrumb сверху, контент по центру через `md:my-auto`) — убрана полоска следующей секции над сгибом: `school-shows`/`birthdays`/`characters`; короткие header-страницы (about/services/gallery/shows/pricing/booking) намеренно не трогали (нет фото-hero — будет пустота). (4) CTA «Book Miss Lana» → «Book a show» везде (`BOOK_CTA.label`, `Hero`, `app/page.tsx` heading+meta, `BookingCTABand` дефолт «Ready to book your show?», `Nav` дефолт + styleguide + комментарии). e2e на эти строки не завязаны. `pnpm run ci:exact` зелёный (build 22 стр.).
- **Применение финального копирайта (TUNG `BUILD_MISS_LANA_COPY_FIXES_001`, v2-full, 2026-06-23).** Применена утверждённая владельцем финальная копия (источник — `docs/reports/2026-06-23-final-copy.md`), только текст + тривиальные alt/title правки, без новых страниц/раскладки/компонентов/схемы. (1) Удалены ВСЕ ~49 видимых литералов «Placeholder/Temporary copy» в 15 файлах (`lib/site.ts`; home-блоки `Hero`/`FormatExplainer`/`ServiceLineCards`/`HowItWorksAreas`/`B2BTeaser`/`B2CTeaser`/`PersonaIntro`/`BookingCTABand`; страницы `services`/`school-shows`/`birthdays`/`characters`/`gallery`/`pricing`/`about`). (2) `lib/shows.ts` — 8 синопсисов+тизеров → финал (титулы/слаги без изменений). (3) `lib/site.ts` — `SERVICE_LINES` ×4 + `VALUES` ×3 финал; **прайс-флор `PRICING_TIERS` строка 1 `$300–350` → `$350`** (публично нет суммы ниже $350). (4) **Правило расстояния опубликовано:** бесплатно в радиусе 30 миль от LA, далее котируется по расстоянию (без $-сумм) — на `/pricing` (блок Distance переписан, убраны `Surcharge amounts pending`/«placeholder presentation», снят неисп. импорт `Tag`) + лендингах `services`/`school-shows`/`birthdays`/`characters` + `HowItWorksAreas` + `/booking`. (5) `/about` — миссия/30+/труппа/ценности финал; **бэкстори ЯВНО называет украинские корни** (тепло, один раз; бренд/SEO/визуал нейтральны; Россия не упоминается); мини-история Miss Lana оставлена как помеченная `🔴 TEMPORARY` строка (без выдумывания биографии). (6) `PersonaIntro` alt «founder and host» → «the theater's host and storyteller» (владельца как founder не утверждаем). (7) Home `<title>` «Live children's theater…» → «Live theater that comes to you» (как H1); `lib/seo.ts` description «live-costumed» → «live costumed children's theater». (8) Снята нота «Synopsis is placeholder copy…» на странице шоу; удалён мёртвый `components/shell/StubPage.tsx`. **Не трогали (pre-launch):** `noindex`, превью-бейдж, плейсхолдер-имагери, соцссылки-заглушки, формат-фильтр шоу. e2e правок не потребовалось (ассерты ссылались на сохранённые H1/заголовки). `pnpm run ci:exact` + `pnpm test:e2e` зелёные (42 passed). Находки/🔴 — `docs/reports/2026-06-23-copy-fixes-build-findings.md`.

## 2026-06-25

- **Canon sync: Theatre/domain/contact lock (TUNG `MISS_LANA_CANON_SYNC_001`).** Core/operational docs synchronized to **Miss Lana's Fairy-Tale Theatre**, live primary domain `misslanatheatre.com`, primary email `info@misslanatheatre.com`, primary phone **(323) 903-2039**, `misslanatheater.com` as protective alternate redirect only, and SMS/email/WhatsApp + 1-2 business-day response workflow. Added pre-edit inventory, final report, and reviewer-grade review artifacts under `docs/reports/` and `docs/reviews/`.
- **Canon finalization (TUNG `MISS_LANA_CANON_FINALIZATION_002`).** Finalized active documentation canon: `/school-shows` locked as canonical B2B route, current-vs-legacy site status clarified, project-owned public copy standardized to Theatre, reviews/testimonials moved into launch-required `[OWNER/CONTENT]` scope, and `@phosphor-icons/react` locked as current icon dependency. Added finalization matrix/report/review artifacts.
- **Canon final cleanup (TUNG `MISS_LANA_CANON_FINAL_CLEANUP_003`).** Removed the last verified root/core doc contradictions: CLAUDE now describes a marketing lead-generation site for a travelling LA children's theatre, STATUS distinguishes live deployment from pre-launch/noindex launch state and points active development to the repository root, README moves next work to pre-launch/launch execution, and sitemap clarifies that form UI exists while production delivery is still unverified. No duplicate file cleanup was performed; prior BRAND duplicate warning was false.

## 2026-06-26

- **Галерея: раскладка ассетов по содержимому + изоляция подозрительных + gitignore (2026-06-26).** Разобран сырой дамп `public/images/1/` (58 фото + 4 видео) — каждый файл просмотрен (4 параллельных vision-агента) и **переименован по содержимому** + разложен в `public/images/gallery/{shows,troupe,children,backstage,clips}/` (имена вроде `bee-character-posing.jpg`, `kids-cheering-with-bubbles.jpg`). `lib/gallery.ts` переписан с placeholder-scaffold на реальные ассеты: курируемый **on-brand** сабсет (26 фото — живые костюмированные актёры, труппа, дети), `GalleryGrid`/`/gallery` отдают реальные фото через `next/image` (`object-cover` = «нежный кроп», masonry, без CLS). **Brand-курирование:** кадры с кукольной ширмой/перчаточными куклами (крокодил/ёж/волк/свинки), дубли, размытые и 4 видео (контент непроверен) — **отсортированы и лежат на диске, но НЕ показываются** (бренд залочен как живой костюмированный театр, не кукольный — 🔴 владельцу примирить позиционирование с реальным репертуаром). **Подозрительные изолированы** (не удалены) из `public/` в `quarantine/`: `COMPETITOR-magic-castle-morozko-poster.jpg` (постер конкурента) + `scraped-tilda-assets/` (файлы с префиксом `tild*` = выгрузка с Tilda-сайта + `red.jpeg`) + README с провенансом. `next-env.d.ts` добавлен в `.gitignore` + `git rm --cached` (Next регенерит его каждый билд — перестанет «пачкать» дерево). e2e gallery-тест обновлён (категории Shows/Troupe/Children + реальные `<img>` вместо «pending»). `pnpm run ci:exact` зелёный; live :3000 — оптимизатор `200` по всем 26 фото. 🔴 Остаётся: видео-обзор+подключение, owner-решение по puppet-кадрам, тяжёлые `*-original.heic/.jpg` в `public/` — кандидаты на вынос.
- **Главная: GalleryTeaser подключён к реальным фото.** `components/blocks/GalleryTeaser.tsx` переписан с 6 хардкод-плейсхолдеров «Photo — pending» на первые 6 курируемых фото из `lib/gallery.ts` (`next/image` `fill` + `object-cover`, тот же источник что `/gallery`); убран таг «Photos pending». Lint/typecheck зелёные; live :3000 — 6 уникальных фото в тизере. Остаются 4 «Photo — pending» в `FeaturedShows` (per-show постеры — 🔴 ассет по каждому шоу от владельца).
- **Главная/школы: карточки шоу + B2B/B2C тизеры подключены к реальным фото.** Всем 8 шоу в `lib/shows.ts` проставлен `image` из галереи по содержанию (Little Red Riding Hood → красная накидка с корзинкой, The Winter's Gift → дети с Сантой у ёлки, Gingerbread Man → погоня, и т.д.); `ShowCardGrid` теперь передаёт осмысленный `mediaAlt`. Убраны теги «Photos coming soon» (`FeaturedShows`) и плейсхолдеры «Assembly / classroom photo» (`B2BTeaser` → `host-with-seated-children.jpg`) и «Birthday party photo» (`B2CTeaser` → `host-dancing-with-girl.jpg`). `pnpm run ci:exact` зелёный; live :3000 — 0 «Photo — pending» / 0 «Asset pending» на главной и `/school-shows`. Соответствие фото↔шоу — рабочее (🔴 владелец уточнит точные кадры по каждому шоу + per-show формат). Длительность «35–50 min» не трогал (отдельная копи-задача — должно стать ~60 мин).
- **Детальные страницы шоу читают `show.image` (единый источник истины) + кроп-фокс + правка подбора.** Главное: `/shows/[slug]` имела **собственный хардкод-плейсхолдер** «Show photo — pending» и игнорировала `show.image` — теперь hero рендерит реальное фото из `lib/shows.ts` (как карточки), иначе плейсхолдер. Добавлен `Show.imagePosition` ("top"/"center"/"bottom") → проброшен в `Card.mediaPosition` (`object-top/-bottom`), чтобы у портретных кадров не срезалось лицо. **Подбор:** Little Red Riding Hood → `red-cape-character-with-basket-2.jpg` + `top` (лицо в кадре); у **Cinderella** и **The Gingerbread Man** фото убраны (стояли непрофильные кукольные сцены) → плейсхолдер «фото ожидается» до owner-ассета. Убран таг «Photos coming soon» на хабе `/shows`. `pnpm run ci:exact` зелёный.
- **About: добавлена ссылка в галерею команды.** В блоке «Meet the troupe» под групповым фото добавлена кнопка «See the troupe in our gallery» → `/gallery` (раньше со страницы About не было входа в фото команды). Портреты артистов остаются плейсхолдерами (нужны owner-фото конкретных людей, не персонажей).

## 2026-06-27

- **Prelaunch-pipeline TUNG-задачи: нумерация + переименование папки (chore).** Папка `tasks/miss-lana-tungs-2026-06-27/` → `tasks/miss-lana-prelaunch-pipeline/`; 7 TUNG-файлов получили префиксы `01_`…`07_` в порядке выполнения (вывод из README + `prerequisites.must_have_completed`). Внутренние `task`-ID и ссылки в зависимостях не трогали (префикс — только в имени файла). README пайплайна обновлён. Ветка `chore/tung-prelaunch-pipeline-ordering`.
- **Owner-answers canon-sync (TUNG `CANON_SYNC_MISS_LANA_OWNER_ANSWERS_001`, prelaunch-pipeline #1).** Канон фактов синхронизирован с подтверждёнными ответами владелицы (Svitlana Grygoryshyna, 2026-06-25…26), **без изменений сайта/кода**. Правки: длительность «35–50 мин» → **около часа** (~30 мин спектакль + ~30 мин интерактив) в `PROJECT_BRIEF`, `01_CONTENT_INVENTORY` (формат-строка + 8 строк таблицы), `02_POSITIONING`, `SITE_STRUCTURE_AND_BLOCKS`; труппа «4 артиста» → **3–4 артиста** (там же + `BRAND.md`); публичная цена сведена к **«From $350»** + custom-quote, градация по числу детей помечена как **внутренняя/не публикуется** (`PROJECT_BRIEF`, `03_SITEMAP`, `SITE_STRUCTURE`); правило расстояния подтверждено (greater LA/Orange County free, дальние — по расстоянию); добавлены setup/venue факты (~20 м², ~15–20 мин, свой звук) с маршрутизацией на будущую страницу Planning Your Event; **Svitlana = владелец и директор**, California с 2022 (сняты «подтвердить»-хеджи). Решение «куклы внутри» подтверждено и записано, но переписывание «no puppet» guardrails — отдельным пассом. `STATUS.md` — снапшот + открытые вопросы + порядок 7 задач пайплайна. **Новые артефакты:** `docs/core/OWNER_ANSWERS_DECISION_RECORD.md` (confirmed-public / internal / unsafe / unresolved) + единый отчёт пайплайна `docs/reports/CANON_SYNC_MISS_LANA_OWNER_ANSWERS_001.md` (по каждой из 7 задач дописывается своя секция — чтобы не плодить файлы). Верификация: `pnpm governance` 0 issues, `git diff --check` чисто, stale-grep без живых нарушений. Находки: NOTE (BRAND troupe-fix для консистентности), LOW (DESIGN_SYSTEM `~40 мин` пример — отложен в Task 02), NOTE (формат-guardrails и code-литерал «35–50 min» в `lib/shows.ts` — Task 02). Без BLOCKER/HIGH/MEDIUM.
- **Глобальная коррекция публичной копи в коде (TUNG `FIX_MISS_LANA_GLOBAL_PUBLIC_COPY_001`, prelaunch-pipeline #2).** Код приведён в соответствие с каноном из #1, при сохранении лёгкой Lantern-Light структуры (без редизайна/новых маршрутов/бэкенда формы; `noindex` не трогали). (1) **Бренд → «Theatre»** во всех owned-brand рендерингах: `lib/site.ts` (BRAND.name/descriptor + domain → `misslanatheatre.com`), `lib/seo.ts` (SITE.name), `app/layout.tsx`, `Nav.tsx`, `accent.ts`, страницы `services/gallery/about/shows/booking/pricing` + `shows/[slug]`, заголовок `globals.css`. Generic-SEO «theater» («live theater», schema `TheaterEvent`) намеренно сохранён. (2) **Один публичный телефон** — `PHONES` и `SITE.phones` сведены к primary `(323) 903-2039`; reserve `(213) 282-1054` убран из всех публичных surface + из schema (`telephone` теперь primary, был reserve — реальный баг structured-data). (3) **Длительность → «about an hour» / «~60 min»** в shared-источниках (`FACTS.showLength`, `lib/shows.ts LENGTH`) + все хардкод-литералы на страницах; `FormatExplainer` теперь передаёт ~30+~30 + сценография/реквизит/свой звук + труппа 3–4. (4) **Travel** → «free across greater LA & Orange County; farther quoted by distance» (убрано «free within 30 miles» в ~9 файлах, без $-сумм). (5) **Прайс: убрана публичная таблица tiers** ($350/$400/$500/$600 по числу детей — нарушение locked-constraint): удалён `PRICING_TIERS`, добавлен `QUOTE_FACTORS` + панель «From $350», H1 `/pricing` → «From $350». (6) **Честный demo-нотис формы** сохранён (реальная отправка — TUNG 5). e2e: обновлён ассерт H1 `/pricing`. Верификация: `pnpm lint`/`typecheck`/`governance` (0 issues)/`build` (22 маршрута)/`test:e2e` (**42 passed**)/`git diff --check` — все зелёные; built-HTML без `$400/500/600`, без reserve-телефона, без «35–50» на публичных маршрутах. Отчёт — секция «Task 02» в `docs/reports/CANON_SYNC_MISS_LANA_OWNER_ANSWERS_001.md` (+ pointer `docs/reports/FIX_MISS_LANA_GLOBAL_PUBLIC_COPY_001.md`). Находки: HIGH (tier-таблица + reserve-телефон в schema — исправлено), LOW (внутренняя `/design`-превью со старой копи — вне scope, отложено). Без BLOCKER/MEDIUM.
- **Лёгкое распределение контента по страницам (TUNG `IMPLEMENT_MISS_LANA_LIGHTWEIGHT_CONTENT_STRUCTURE_001`, prelaunch-pipeline #3).** В существующие страницы добавлен минимум подтверждённого контента, story-led и легко, без редизайна/новых блоков-стен; логистика НЕ дублируется (уходит на будущую Planning Your Event, Task 04). (1) **Birthdays** — добавлен компактный блок «For the birthday child» (4 карточки: приветствие по имени + опц. маленькая роль; «Happy Birthday» + финал; маленький подарок — мягкая игрушка + шарики; опц. костюмированный персонаж «subject to availability») — формулировки availability, не обязательность («as much or as little as they'd like»); добавлена строка про семейные фото. (2) **School Shows** — +2 OFFER-карточки (Flexible scheduling: последовательные шоу для разных возрастных групп; Best in smaller groups: рекомендация меньшей аудитории **без** жёсткого максимума) + FAQ про расписание; SEL/values уже были; без procurement/insurance/doc-заявлений. (3) **Characters** — блок «Optional extras» (face painting · standalone bubble show · character visit), всё optional/availability-based, inventory-neutral (без списка гарантированных персонажей). (4) **Show detail** — в компактный Show Details aside добавлена строка «Troupe — 3–4 artists»; story лидирует; per-show формат НЕ добавлен (owner-gated), ссылка на Planning Your Event **отложена** (маршрут ещё не создан — Task 04). (5) **Home FormatExplainer** и **Pricing** уже соответствовали (Task 02) — без изменений. Новый артефакт: `docs/reports/miss-lana-page-content-distribution-matrix.md` (per-page budget + fact→page). Верификация: `pnpm lint`/`typecheck`/`governance` (0 issues)/`build` (22 маршрута)/`test:e2e` (**42 passed**)/`git diff --check` — зелёные; manual: на Home/show-страницах нет setup/parking/weather-стены, `/pricing` только `$350`, birthday-копи не обязательная, extras inventory-neutral. Отчёт — секция «Task 03» (+ pointer `docs/reports/IMPLEMENT_MISS_LANA_LIGHTWEIGHT_CONTENT_STRUCTURE_001.md`). Находки: только NOTE (pre-existing birthday-FAQ про «space and power» — оставлено; Planning-ссылка и per-show формат отложены). Без BLOCKER/HIGH/MEDIUM.
- **Страница Planning Your Event (TUNG `BUILD_MISS_LANA_EVENT_PLANNING_FAQ_001`, prelaunch-pipeline #4).** Создан один дружелюбный лёгкий маршрут `app/planning-your-event/page.tsx` — единый дом для общей venue/логистики, чтобы Home и маркетинг-страницы оставались story-led. Прогрессивное раскрытие: 5 групп аккордеонов (Space & setup · Indoors & outdoors · Parking & access · Photos · Scheduling & booking), **все закрыты на загрузке** (без стены текста), 10 вопросов — **только подтверждённые факты** (decision record §1): ~20 м² (≈215 sq ft, как рекомендация, не жёсткое правило), сетап/сборка ~15–20 мин, свой звук/обычно без розетки, indoor+outdoor (улица — ровно/тень/без ветра, indoor-backup рекомендован), parking host-arranged (платная может быть на клиенте, всегда подтверждается заранее), фото снимают родители/учителя (перформеры детей не снимают), последовательные шоу для разных возрастов (меньшая аудитория рекомендована, **без** жёсткого максимума). Нерешённые политики (отмена/депозит/возврат) **смягчены до «confirmed when you book»** — не публикуем как жёсткую политику (§4). Дружелюбный H1, intro + booking CTA сверху, **один** объединённый `FAQPage` ровно из 10 видимых Q/A (без дублей схемы), Breadcrumb→BreadcrumbList, **noindex**. Скудные вторичные текст-ссылки из School Shows / Birthdays / Booking / show-detail + low-priority ссылка в footer (`FOOTER_LINKS`, не nav). e2e +5 (200/H1/noindex, FAQPage+BreadcrumbList, closed-on-load+keyboard, footer-link, guard на запрещённые policy-слова). Верификация: `pnpm lint`/`typecheck`/`governance` (0 issues)/`build` (23 маршрута)/`test:e2e` (**47 passed**)/`git diff --check` — зелёные; built-HTML аудит: 10 видимых вопросов = 10 schema Question, ровно один FAQPage, без deposit/refund/insurance/supervision-слов. Отчёт — секция «Task 04» (+ pointer `docs/reports/BUILD_MISS_LANA_EVENT_PLANNING_FAQ_001.md`). Только NOTE-находки. Без BLOCKER/HIGH/MEDIUM.
- **Production lead-pipeline (TUNG `IMPLEMENT_MISS_LANA_PRODUCTION_LEAD_PIPELINE_001`, prelaunch-pipeline #5).** Демо-форма заменена реальным серверным пайплайном, без новых зависимостей. Новое: `app/api/lead/route.ts` (Node-runtime POST: honeypot `company` → per-IP rate-limit 5/60s → 16KB cap → **авторитетная серверная валидация** → durable persist → notify → честный JSON; **`ok:true` только после durable-persist**, никогда при ошибке валидации/абьюза/стора); `lib/leads.ts` (чистый контракт: `validateLead`/`buildLead` (US-date→ISO, trim/cap), `makeInquiryId` `ML-XXXXX`, `formatLeadSummary`; privacy-minimal — имена детей не требуем); `lib/notify.ts` (server-only: **durable store ПЕРВЫМ** = гарантия восстановления, затем provider-agnostic email-webhook на `info@misslanatheatre.com`, затем опц. Telegram; не бросает, возвращает per-channel outcomes); `lib/analytics.ts` (no-op-safe события lead_success/error/phone_click; allow-list — **без PII**); `lib/env.ts` (+`leadNotifyEmail` default info@misslanatheatre.com, `leadEmailWebhookUrl/Token`, `leadStoreDir` default `.leads`; Telegram стал опциональным; весь доступ к env — через lib/env). `LeadForm` переписан под endpoint: states submitting/success-after-accept(+reference id)/recoverable-error+retry; honeypot off-screen+tabindex=-1+aria-hidden; UTM читается из URL на submit (без useSearchParams-Suspense). **Убран ложный «demo — nothing is sent»** на форме и в 3 CTA-sub (стал неправдой — форма реально сохраняет); success-копи обещает только «Miss Lana ответит» (не обещаем клиентское письмо). `.gitignore` +`.leads/`; `.env.example` +placeholder-ключи; footer-телефоны трекают phone_click. **Live-тест локально:** valid→`{ok,id}`+durable JSON, invalid→422, honeypot→ignored+не сохранён, 7 rapid→429, email-skip честно залогирован, **без PII в логах**. Верификация: `lint`/`typecheck`/`governance`(0)/`build`(ci:exact exit 0, `/api/lead`=ƒ)/`test:e2e` (**51 passed**, +4 lead)/`security/secret-scan.sh` (чисто, staged-scan чисто)/`git diff --check` — зелёные. Артефакт: `docs/operations/LEAD_PIPELINE_RUNBOOK.md` (без секретов). Отчёт — секция «Task 05» (+ pointer). **⚠️ Launch-gate (HIGH, документирован):** живая email-доставка в инбокс НЕ проверена (нет provider-креденшелов в этой среде) — адаптер полностью тестируемый, durable store не теряет лиды; перед платным трафиком установить `LEAD_EMAIL_WEBHOOK_URL` и пройти runbook-чеклист. NOTE: Turbopack NFT-warning (dynamic fs-path, build exit 0), in-memory rate-limit (single instance). Без BLOCKER/MEDIUM.
